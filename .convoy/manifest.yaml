# Convoy manifest — tracks files authored by Convoy.
# Developer source code is never modified.
version: 1
repo: softpharmamanager
platform: vercel
framework: next.js
authored:
  - path: vercel.json
    purpose: framework=next.js, region=iad1, prisma generate in build
  - path: .env.schema
    purpose: declares required runtime env vars
ignored:
  - "MASTER SHEET FOR LOVABLE_SUPABASE DOWNLOAD (1).xlsx"
  - book_import_checkpoint.json
  - .env
